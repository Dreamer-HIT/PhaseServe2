# Local Utility Scripts

This directory is reserved for active local utilities.

The old end-to-end figure script has been moved to `scripts/archive/` because it depends on legacy Stage 4D figure data. New paper figures should use curated result summaries from `results/` and the current experiment protocol.
