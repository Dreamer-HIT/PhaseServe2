# Result Summaries

This directory stores curated, lightweight summaries only. Raw benchmark traces, server logs, JSONL event streams, model files, and temporary sweep directories should remain on the data disk of the experiment server.

Current curated result set:

- `stage4l_opt_sharegpt_bridge_budget/`: OPT-13B + ShareGPT bridge-budget repair summaries, including the clean continuous validation window and high-rate stress checks.
